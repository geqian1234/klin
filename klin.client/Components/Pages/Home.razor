@page "/"
@inject ISearcher searcher
<MudThemeProvider />
<MudPopoverProvider />
<MudButton Variant="Variant.Filled" OnClick="Send">Default</MudButton>
<MudText>@httpv</MudText>
<MudTest/>


@code{
    private string httpv = "";
    private async Task Send()
    {
        ServiceResponse<FilteredSearchResultModel> ret=await searcher.Search("夜的第七章");
        if (ret.Status)
        {
         //   httpv = ret.Value;

        }
    }


}